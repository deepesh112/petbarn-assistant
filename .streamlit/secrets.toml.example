# Copy to .streamlit/secrets.toml and fill in your key (that file is gitignored).
# On Streamlit Community Cloud, paste these lines into Settings -> Secrets instead.

# Required to chat. Free keys: https://console.groq.com/keys
GROQ_API_KEY = "gsk_your_key_here"
